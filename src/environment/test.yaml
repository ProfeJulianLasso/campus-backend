scope:
  development: false
  production: false
  test: true

mongo:
  user: root
  password: password
  database: ddd
  host: localhost
  port: 27017
  dsn: mongodb:${self:mongo.user}:${self:mongo.password}@//${self:mongo.host}:${self:mongo.port}/${self:mongo.database}?authMechanism=DEFAULT
